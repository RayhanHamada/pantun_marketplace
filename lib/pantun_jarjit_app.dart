import 'package:flutter/material.dart';

class PantunJarjitApp extends StatelessWidget {
  const PantunJarjitApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      title: 'Pantun Jarjit App',
    );
  }
}
